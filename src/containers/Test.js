import React from 'react';
import { Router, Route, IndexRoute, Link, browserHistory } from 'react-router';

const Test = props => {
  return(
    <div>
      Hello World!
    </div>
  )
}

export default Test;
